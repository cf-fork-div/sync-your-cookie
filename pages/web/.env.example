# Web viewer — local dev only (pnpm dev:web)
# Production uses Cloudflare runtime WEB_ACCESS_PASSWORD / WEB_BASE_PATH (see deploy/cloudflare/.env.example)

VITE_WEB_ACCESS_PASSWORD=dev
VITE_WEB_BASE_PATH=my-cookie-vault
