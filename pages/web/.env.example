# Web viewer — local dev only (pnpm dev:web)
# Production uses Cloudflare runtime WEB_ACCESS_PASSWORD (see deploy/cloudflare/.env.example)
# Optional: WEB_BASE_PATH for custom hidden path; omit for root /

VITE_WEB_ACCESS_PASSWORD=dev
# VITE_WEB_BASE_PATH=my-cookie-vault
